import { DataRouter } from 'react-router';
import { IRouter } from '../../types/router/app-router-types';
export declare function createWindowRouter(router: DataRouter): IRouter;
export declare function registerWindowRouter(router: DataRouter): void;
